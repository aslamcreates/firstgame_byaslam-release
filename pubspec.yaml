name: grow_plants_puzzle_game
description: Grow Plants Puzzle Game developed by Aslam Hameed for Flutter Puzzle Challenge
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# version
version: 1.0.0+1

environment:
  sdk: ">=2.16.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

# The following section is specific to Flutter.
flutter:
  uses-material-design: true
  assets:
    - assets/images/